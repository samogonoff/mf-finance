-- Откат 0033.
--
-- ВНИМАНИЕ: удаление таблиц не возвращает cost_data_cache к исходным ценам —
-- применённые наборы уже наложены на кэш. После откатa нужен полный рефреш кэша
-- из источника, иначе в нём останутся цены из удалённых наборов без какого-либо
-- следа их происхождения.

DROP TABLE IF EXISTS cost_plan_price_set_rows;
DROP TABLE IF EXISTS cost_plan_price_sets;
