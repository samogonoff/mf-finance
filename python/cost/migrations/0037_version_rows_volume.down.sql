-- Откат: убрать «выпуск шт» из строк версий.
--
-- ВНИМАНИЕ: откатывать имеет смысл только вместе с 0036 и вместе с удалением
-- «выпуск шт» из CACHE_COLUMNS в app/db.py. Если колонка останется в списке, а
-- в таблице её не будет, обновление кэша снова упадёт на переприменении версий.

ALTER TABLE cost_calc_version_rows
    DROP COLUMN IF EXISTS "выпуск шт";
