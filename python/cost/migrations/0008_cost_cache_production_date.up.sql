-- 0008 — Добавляем «дата производства» (дата выпуска) в cost_data_cache.
-- Источник: MSSQL [Checks].[dbo].[CostHistory].[дата производства]
-- На фронте отображается как «Дата выпуска».
--
-- После применения миграции существующий кеш остаётся работоспособен;
-- колонка заполнится при следующем плановом или ручном обновлении кеша.

ALTER TABLE cost_data_cache
    ADD COLUMN IF NOT EXISTS "дата производства" TIMESTAMPTZ;
