# Finance · Cost — AGENTS.md

> ⚠ **Scope constraint**: Работаем **только** в `python/cost/`. Редактирование любых файлов за пределами этой директории — строжайший запрет.

## Quick start

```bash
cp .env.example .env          # fill MSSQL/OLAP creds (или COST_MOCK=1)
# Isolated contour (cost-only):
cd swarm && make cost-up      # postgres-cost + python-cost + nuxt-cost + nginx-cost
make cost-logs                # tail all cost services
```

| Service | Cost contour URL | Direct port |
|---------|------------------|-------------|
| FastAPI | http://localhost:8091 | `:8091` |
| Nuxt (cost-only) | http://localhost:3002 | `:3002` |
| Nginx (cost) | http://localhost:8088 | `:8088` |
| Postgres-cost | `psql -h localhost -p 55433 -U cost cost` | `:55433` |

В основном дев-контуре (`cd swarm && make up`) эндпоинты `/api/cost/*` доступны через `api.finance.local/api/cost/` (nginx проксирует на `python-cost:8091`).

## Структура

```
python/cost/
├── app/
│   ├── __init__.py       # пустой
│   ├── main.py           # FastAPI app, lifespan (init/close pool), CORS, access-лог, /healthz
│   ├── routes.py         # все эндпоинты раздела
│   ├── db.py             # asyncpg (postgres-cost) + pyodbc (MSSQL/OLAP)
│   ├── logship.py        # логи JSON в stdout + async-трансляция в Logstash (ELK)
│   └── mocks.py          # заглушки для COST_MOCK=1
├── migrations/
│   └── 0001_cost_init.{up,down}.sql
├── nuxt-layer/
│   ├── nuxt.config.ts
│   ├── pages/cost/index.vue   # SPA-страница «Себестоимость»
│   ├── components/CostMultiSelect.vue
│   ├── middleware/cost-redirect.global.ts
│   └── plugins/cost-bypass.ts
├── Dockerfile.dev         # python:3.12-slim-bookworm + msodbcsql18
├── requirements.txt       # fastapi, uvicorn, asyncpg, pyodbc, pydantic, python-dotenv
├── init-db.sh             # entrypoint для postgres-cost (применяет миграции)
├── .env.example           # шаблон с комментариями
├── .env                   # реальные креды (в gitignore)
└── AGENTS.md              # этот файл
```

## Логи (`app/logship.py`)

Логи раздела — одна JSON-строка на событие в stdout (базовый канал, его собирает
docker) плюс дубль в Logstash/ELK, если задан `LOGSTASH_HOST` (+ `LOGSTASH_PORT`,
дефолт 5044) в `python/cost/.env`. Пусто → трансляция выключена. `service=finance-cost`;
формат общий с Go-API (`finance-api`) и Nitro-роутами Nuxt (`finance-nuxt`), поэтому в
одном индексе ELK всё лежит единообразно.

Отправка асинхронная (поток-демон + очередь) и НИКОГДА не блокирует запрос:
недоступный Logstash не тормозит API, строки отбрасываются, счётчик потерь раз в
минуту уходит в лог. Access-лог каждого запроса пишет middleware в `app/main.py`
(`request_id`, `route`, `status`, `duration_ms`, `user` из `X-Cost-User`); он же
прокидывает `X-Request-Id` — по нему в ELK сшивается цепочка nuxt → go-api → cost.

Новую диагностику пиши через `log(logging.INFO, "сообщение", поле=значение)` из
`app.logship`, а не `print()` — иначе строка останется только в stdout.

## FastAPI endpoints (`app/`)

Все эндпоинты зарегистрированы с префиксом `COST_API_PREFIX` (по умолчанию `/api/cost`).

| Метод | Путь | Описание | Источник данных |
|-------|------|----------|-----------------|
| `GET` | `/filter-options` | Каскадные значения фильтров (DWH) | OLAP `[DWH].[dim].[groups]` |
| `POST` | `/load-data` | Сырые данные с пагинацией (limit/offset) | MSSQL `[Checks].[CostHistory]` |
| `POST` | `/aggregated` | GROUP BY с AVG/SUM по CostHistory | MSSQL `[Checks].[CostHistory]` |
| `POST` | `/details` | Детализация по модели с GROUP BY | MSSQL `[Checks].[CostHistory]` |
| `GET` | `/price-levels` | Справочник уровней цен | MSSQL `[Gpartner].[s_price_level]` |
| `POST` | `/save-changes` | Сохранить одно изменение цены | OLAP `[FinSandBox].[CostHistory_Changes]` + локальный аудит |
| `POST` | `/save-batch` | Массовое сохранение | OLAP `[FinSandBox].[CostHistory_Changes]` + локальный аудит |
| `POST` | `/calc-sign-copy` | Создать калькуляцию с другим признаком (копия расчёта) | `cost_manual_calc` |
| `GET` | `/calc-sign-copy` | Журнал созданных копий | `cost_manual_calc` |
| `DELETE` | `/calc-sign-copy/{batch_id}` | Удалить созданную копию целиком | `cost_manual_calc` |
| `GET` | `/multipack` | Состав мультипака + актуальная себестоимость одиночек, детект устаревания | `cost_multipack*` + `cost_data_all` |
| `PUT` | `/multipack` | Создать/переписать состав | `cost_multipack*` |
| `DELETE` | `/multipack` | Распустить пак (строки расчёта остаются в версии) | `cost_multipack*` |
| `POST` | `/multipack/build` | Собрать/пересобрать строки расчёта (версию НЕ создаёт) | `cost_multipack*` + `cost_data_all` |
| `GET` | `/multipack/candidates` | Поиск калькуляций-одиночек для состава | `cost_data_all` |
| `GET` | `/multipack/list` | Журнал мультипаков и сборок | `cost_multipack*` |
| `GET` | `/multipack/usage` | В какие паки входит эта одиночка | `cost_multipack_item` |
| `GET` | `/articul-replace/recipients` | Адресаты рассылки о замене артикула по сегментам (ЧНИ / остальное) и настроен ли SMTP | `cost_articul_replace_recipients` |
| `POST` | `/articul-replace` | Поставить/снять отметку «нужна замена артикула»; при установке — письмо операторам и сообщение в Битрикс | `cost_articul_replace*` + SMTP + site_api |
| `GET` | `/reg713/articuls` | Поиск артикула-аналога (`q` от 2 символов): артикул, модель, наименование | Gpartner `S_MODELI` |
| `GET` | `/reg713/analog-prices` | Розница и опт аналога по правилам главной таблицы (`source`: calc / dwh / gpartner) | `cost_data_all` + OLAP + `S_MODELI` |
| `GET` | `/reg713/card` | Карточка согласования по 713 и её история | `cost_reg713*` |
| `GET` | `/reg713/list` | Отчёт: все отмеченные на согласование изделия с ценами, аналогами и решениями (`all=1` — и снятые) | `cost_reg713` + цены как в таблице |
| `POST` | `/purchase/import` | Импорт КПСС закупной готовой продукции из портала БМ (`dry_run` — только посчитать) | OLAP `mfportal.bm_chl_*` → `cost_manual_calc` + `cost_purchase_cost` |
| `GET` | `/purchase/import/runs` | Журнал запусков импорта | `cost_purchase_import_run` |
| `GET` | `/purchase/detail` | Раскладка себестоимости закупной калькуляции (+ план КПСС для ПФКСС) | `cost_purchase_cost` |
| `GET` | `/purchase/rates` | Курсы НБ РБ к бел. рублю на дату (USD, EUR, RUB, KZT, UZS, CNY) | OLAP `[DWH].[dim].[valuta1].KURS_BANK` |
| `GET` | `/purchase/currencies` | Валюты, доступные для прихода и статей накладных | — |
| `POST` | `/purchase/hs-codes` | Коды ТН ВЭД по парам модель+артикул | Gpartner `S_MODELI.KTNVED` |
| `POST` | `/purchase/invoice/preview` | Распределить накладные инвойса по строкам без сохранения | — |
| `GET` | `/purchase/invoices`, `/purchase/invoice/{id}` | Список инвойсов прихода / один с строками | `cost_purchase_invoice*` |
| `POST` | `/purchase/invoice` | Создать/обновить черновик инвойса (id в теле — обновление) | `cost_purchase_invoice*` |
| `POST` | `/purchase/invoice/{id}/apply` | Применить: создать калькуляции ПФКСС и раскладку | `cost_manual_calc` + `cost_purchase_cost` |
| `DELETE` | `/purchase/invoice/{id}` | Удалить черновик (применённый — нельзя) | `cost_purchase_invoice*` |
| `POST` | `/reg713/card` | Сохранить карточку (требуется / вид / аналог с ценами / решение исполкома) | `cost_reg713*` |
| `GET` | `/healthz` | Health check | — |

Таблица выше — исторический срез первых эндпоинтов; полный список смотри по
`@router` в `app/routes.py`.

### Детали эндпоинтов

#### `GET /filter-options`
- **Новый источник**: OLAP `[DWH].[dim].[groups]` (справочник групп), а не CostHistory.
- Фильтры: `brand_manager` (из `BRAND_FIO`), `level01`–`level05` (из `gr1-gr5`/`group1-group5`), `calc_sign` (статический).
- `level01`–`level05` возвращают `[{id, text}]` для Select2-совместимости.
- `brand_manager` и `calc_sign` — плоские `string[]`.
- `calc_sign` статический: `["ПКПСС", "КПСС", "ПФКСС", "ФКСС"]`.
- Убраны из фильтров: `country`, `family`, `season`, `model`, `articul`.
- Каскад: полный двусторонний между brand_manager и Level01-05.
- Параметры: query params (ключи фильтров, `id` значения для уровней).

#### `POST /load-data`
- Новый эндпоинт для сырых данных с пагинацией.
- Body: `{ date_from, date_to, brand_manager: [...], level01: [...], ..., limit, offset }`
- `limit` макс. 5000, по умолчанию 1000.
- Возвращает: `{ data: [...], count, total, offset, limit }`.

#### `POST /aggregated`
- GROUP BY: `Бренд-менеджер, Модель, Артикул, Признак калькуляции, дата расчета, Уровень цен, Страна пр-ва, Семья, Сезон`.
- AVG-поля: `Розничная цена по уровню, руб.`, `Отпускная цена по уровню, руб`, `Розничная цена по уровню, USD.`, `Отпускная цена по уровню, USD.`, `Пошив, руб.`, `Пошив, USD.`, `Раскрой, руб.`, `Раскрой, USD.`
- SUM-поля: `Основные материалы, руб.`, `Основные материалы, USD.`, `Вспомогательные материалы, руб.`, `Вспомогательные материалы, USD.`, `Декоры, руб.`, `Декоры, USD.`, `Себестоимость, руб.`, `Себестоимость, USD.`
- Фильтры: все 12 полей (level01-05, brand_manager, country, family, season, calc_sign, model, articul).

#### `POST /details`
- **Изменён**: принимает `model` (строка), не `model + articul`.
- Body: `{ model, date_from?, date_to?, calc_sign? }`
- Делает GROUP BY по `дата расчета, Признак калькуляции, Модель, Артикул, Наименование модели, Номер задания производства`.
- Серверный расчёт: `Себестоимость, руб.` (сумма статей), `Наценка, руб.`, `Наценка, %`, `Маржинальность, %`.
- Новые поля: `Номер задания производства`, `Вязание, руб.`, `Декор, руб.`.

#### `POST /calc-sign-copy` — калькуляция с другим признаком (пункт 1 «Списка доработок»)
- Признак у исходной калькуляции НЕ меняется: создаётся новая калькуляция —
  копия всех строк расчёта с выбранным признаком. Так решил заказчик 27.08.2026.
- Разрешённые признаки: `ПКПСС`, `КПСС`, `ПФКСС` (`CALC_SIGN_COPY_ALLOWED` в
  `app/db.py`). ФКСС не участвует — это история факта, в DWH не пишется.
- Право `cost:calc_sign_copy` — только роли «Калькулятор» и «Full Admin»
  (миграция 0043). Бренд-менеджеру и ПЭО кнопка не показывается.
- Копия создаётся до расценки: если по исходной калькуляции уже есть цена в
  `cost_price_pending` или согласование ПЭО, эндпоинт отвечает 400.
- Номер задания у копии пустой — согласование ПЭО по ней идёт как по одной
  калькуляции. Если у источника задание есть, копируются строки только его.
- Ответ содержит `clash_with_source`: признак уже есть у калькуляции источника.
  Тогда у копии и у неё будет ОДНА цена — цена в разделе ставится на модель,
  артикул, план и признак. Создание при этом не блокируется (решение заказчика:
  пересчёт может понадобиться и после того, как источник свою калькуляцию
  прислал).
- Правки материалов у копии идут обычным механизмом версий; применение версии
  выбирает таблицу по ключу (`_calc_table` в `app/db.py`), иначе DELETE+INSERT
  ушли бы в кэш источника.

#### `POST /save-changes` и `/save-batch`
- Пишут в OLAP `[FinSandBox].[dbo].[CostHistory_Changes]` и дублируют в локальный `cost_price_changes_audit` (postgres-cost).
- В mock-режиме — только локальный аудит.

#### Мультипаки — `app/multipack.py`, `/multipack/*` (миграция 0049)

Пак (3/5/7 пар в одной единице продажи) — модель-артикул, себестоимость которого
складывается из калькуляций-одиночек плюс материалы упаковки. Слот такой
калькуляции в источнике ЕСТЬ, но пустой: 430A-3839 / B3-263430A «НОСКИ ДЕТСКИЕ
(3 пары)» несёт две строки типа «шт» с нулевой суммой. Заказчик подтвердил
02.09.2026, что паков без слота не бывает (цена пишется обратно в источник по
ключу через процедуру), поэтому калькуляция создаётся не с нуля — наполняется
существующий ключ.

**Строки живут в версии калькуляции, не здесь.** `/multipack/build` только
ГЕНЕРИРУЕТ строки и возвращает их в редактор; сохраняет их обычный
`create-version` / `save-calculation-draft`. Отсюда бесплатно приходят цена,
согласование ПЭО, запись в DWH, блокировки и переживание refresh кэша
(`_reapply_active_versions_to_cache`). Если бы сборка писала в кэш сама, она
обходила бы согласование.

**Свёртка по статьям, а не копирование строк.** Одиночка носков — 20–145 строк;
на пак из трёх пар это 300+ нечитаемых строк. Поэтому на каждую одиночку
генерируется по строке НА СТАТЬЮ: `Норма` = количество штук, `цена материала` =
себестоимость статьи за штуку. Дальше работает штатный `_recalc_cost_buckets`.
Декоры — исключение: у них нормы и цены нет, стоимость задаётся суммой в
«Декоры, руб./USD.» (`_PLAN_DECOR_TYPES`).

**`cost_factor_rub/usd` у сгенерированных строк — всегда NULL.** Коэффициент
источника (миграция 0031) описывает КОНКРЕТНЫЙ материал; у наших строк
произведение точно равно стоимости. Попутно этим же коммитом закрыт баг
`addVersionRow` в `index.vue`: шаблон новой строки копировался с первой строки
калькуляции ВМЕСТЕ с её коэффициентом, а он у трети калькуляций носков далёк от
единицы (25-й процентиль по строкам — 0,000241) — введённая цена молча
обнулялась.

**Состав ключуется на (модель, артикул)** — решение заказчика: пак проходит
ПКПСС → КПСС → ПФКСС одним составом, меняются только суммы одиночек. Снапшот
сумм лежит не на составе, а в журнале сборок `cost_multipack_build`: суммы у
каждого этапа свои, и снапшот на строке состава затирался бы следующим этапом.

**План одиночки — «любой», а не план пака.** Проверено на данных: у пака
`PLAN_ID = 0`, у его одиночек — 6762 / 6982 / 7275 / 7773. Пустой план в составе
значит «взять самый свежий», пустой признак — «этап пака». Пользователь может
зафиксировать и то, и другое (📌 в интерфейсе). Внутри плана берётся задание с
максимальной рублёвой себестоимостью — то же правило, что при простановке цены
(`get_max_calc_cost`).

**Строка состава помечена `свойство3` = `мультипак`.** По маркеру пересборка
отличает свои строки от упаковки (упаковка переносится как есть), а редактор
блокирует ручной ввод: правится состав, а не строка.

**Автопересбора нет.** Изменилась себестоимость одиночки — интерфейс показывает
«было X → стало Y» (`stale` в `GET /multipack`), пересобирает человек. Молча
менять согласованные цифры нельзя.

**Паки исключены из дашборда коммерческой эффективности** (`BASE_CONDITIONS` в
`app/commercial.py`) — их себестоимость собрана из одиночек, которые в базе уже
посчитаны. Условие идёт по составу, а не по колонке витрины: состав правят чаще,
чем идёт `REFRESH cost_calc_mv`. В выпуске (`app/margin.py`) паков нет и так —
производят одиночки, пак только комплектуют.

Право — `cost:multipack` (миграция 0049 выдаёт ролям «Калькулятор», «ПЭО» и
«Full Admin»; у `cost:calc_sign_copy` ПЭО исключён намеренно — это разные
решения заказчика).

#### Семафор этапов (пожелание № 2) — колонка «Этап» главной таблицы

Один значок на строку показывает, где калькуляция в процессе; считает его
`rowStage()` в `index.vue`, и порядок проверок там — от конца процесса к
началу, потому что признаки накладываются (у записанной в DWH строки остаётся и
статус ПЭО, и заявка на цену):

| Значок | Этап | Признак в строке `/aggregated` |
|---|---|---|
| 🟣 | загружено в DWH («лису») | `_in_dwh && !_reopened` |
| 🔴 | отклонено руководителем ПЭО | `peo_status == 'rejected'` |
| 🟡 | цена БМ отправлена на корректировку | `peo_status == 'returned'` |
| 🔵 | цена установлена бренд-менеджером | `_has_pending` (заявка в `cost_price_pending`) |
| 🟢 | себестоимость утверждена экономистом | `peo_status == 'approved'` |
| ⚪ | ещё не согласована | ничего из перечисленного |

Переоткрытая админом калькуляция (`_reopened`) показывает не «в DWH», а текущий
шаг второго круга. Клик по значку открывает окно ПЭО, как раньше. Фильтр
«Статус согласования» остался серверным (`peo_filter` в `get_aggregated`,
`routes.py`) и этапы 🔵/🟣 пока не различает — при необходимости расширять там.

#### Отметка «нужна замена артикула» и рассылка (пожелание № 4)

Колонка «Замена арт.» — галочка в строке (право `cost:articul_replace`, миграция
0052 выдаёт «Бренд-менеджеру», «ПЭО», «Калькулятору» и «Full Admin»). Решение о
замене артикула принимает бренд-менеджер — это уточнение заказчика 03.09.2026,
поэтому БМ здесь основной носитель права, а не получатель следствий. Ключ отметки — как у статуса
ПЭО: модель, артикул, признак, план, задание (`cost_articul_replace`,
`uq_articul_replace_key`, пустые части хранятся как `''`). Наложение на строки
`/aggregated` — `articul_replace.apply_flags`, поля `replace_*`.

При установке галочки `app/articul_replace.py` шлёт письмо адресатам сегмента:
ЧНИ (`trim("Level 01") IN ('Носки&Колготки','Orodoro')`) → Кочеткова Т., Король Н.;
всё остальное, включая пустой Level 01, → Дащинская А.А. Адресаты — в таблице
`cost_articul_replace_recipients`, а не в коде. Каждая попытка пишется в
`cost_articul_replace_mail`; итог последней — в `notified_at`/`notify_error`
отметки, интерфейс показывает ✉ или ⚠ с причиной. Снятие галочки писем не шлёт.

Почта — SMTP из стандартной библиотеки, переменные `COST_SMTP_*` и `COST_MAIL_TZ`
(см. `.env.example`). **До этой задачи почтового канала в проекте не было**:
без `COST_SMTP_HOST` отметка ставится, а письмо помечается `skipped` — это видно
и в подсказке к заголовку колонки, и в баннере после установки галочки. Сбой
почты отметку не откатывает: повтор рассылки — снять и поставить галочку снова.

**Второй канал — сообщение в Битрикс напрямую через портал.** У адресата в
`cost_articul_replace_recipients.b24_id` может стоять его ID в корпоративном
Битриксе. Тогда, кроме письма, `send_bitrix` шлёт сообщение через site_api тем
же контрактом, которым Go дублирует уведомления кабинета в Б24
(`POST {id, message: "Finance: …"}` + Basic Auth, ответ `{"success": bool}`).
Переменные `SITE_API_NOTIFY_URL/USER/PASSWORD` — те же имена, что в корневом
`.env`, но читаются из `python/cost/.env`. **Почему не через кабинет:** канал
Go-API `/internal/notifications` адресует по `user_id` кабинета, а эти адресаты
в Finance Cabinet не заходят (заказчик, 03.09.2026) — номера у них не появится.
Первый вариант с `user_id` отброшен по этой причине. В журнал
`cost_articul_replace_mail` каждый канал пишется своей строкой (`channel`:
`email` | `bitrix`); `notified_at` отметки — дошло хотя бы одним каналом,
`notify_error` — что не дошло и почему (например, «Битрикс — user not found
(ID 1002)» при частичной доставке).

#### Согласование цен по постановлению 713 (пожелание № 8) — `app/reg713.py`

В Беларуси цены на часть ассортимента регулируются постановлением Совмина
№ 713: новинка и повышение цены требуют согласования с исполкомом. Колонка
«713» главной таблицы показывает состояние карточки (`·` не требуется, 📋
требуется без решения, ✅ согласовано, ❌ не согласовано), клик открывает окно.
Право `cost:reg713` — миграция 0053 выдаёт «Бренд-менеджеру», «ПЭО»,
«Калькулятору» и «Full Admin» (решение заказчика 03.09.2026: карточку ведут все
рабочие роли; без права только «Просмотр»).

**Ключ карточки — модель + артикул** (`uq_reg713_key`), без признака
калькуляции, плана и задания — решение заказчика 03.09.2026: «карточка должна
быть привязана вообще к модели-артикулу». Исполком согласует цену изделия, а не
калькуляцию, одно решение действует на все планы и этапы пары. Это крупнее ключа
цены (4 поля) и статуса ПЭО (5 полей) — не «уточнять» его; фронт после
сохранения обновляет все строки пары. Первый вариант с ключом цены отброшен.

**Поля**: `required`, `kind` (`price_increase` | `novelty`), аналог
(`analog_model/articul/name`), его цены `analog_retail/wholesale` со снимком
источника `analog_price_source` (calc | dwh | gpartner | manual) и времени,
`decision` (`approved` | `rejected` | NULL «не рассмотрено»), `decision_doc`,
`comment`. Каждое сохранение — строка в `cost_reg713_log` (было/стало): решение
исполкома — юридически значимый факт, его переписывание должно быть видно.

**Аналог ищется в Gpartner `S_MODELI`** (132 тыс. записей, 111 тыс. пар
модель+артикул; на пару бывает несколько версий — берём последнюю по
`ITEM_ID`). Архивные (`PR_ARH=1`) не скрываем, помечаем и ставим в конец.

**Цены аналога «по тем же правилам, что в главной таблице»** (требование
заказчика), `analog_prices`: (1) последняя калькуляция аналога в
`cost_data_all` — розница и опт «по уровню»; (2) поверх — утверждённая цена в
DWH по ключу этой калькуляции (`fetch_olap_changes`, при недоступности OLAP —
`cost_price_changes_audit`), как накладывается в `/aggregated`; (3) если
калькуляций нет — плановая из `S_MODELI`: `PRICE_MOPT` как опт, розница из
`PRICE_ROZN`, иначе по уровню цен (`PRICE_TYPE1` → `PRICE_TYPE3`, при нескольких
кандидатах ближайший к опт × 1,4 × (1 + НДС) — без различения «Девочкам /
Мальчикам» ×1,3, у аналога нет Level 01). Цены хранятся снимком: исполком видел
именно эти цифры; кнопка «Обновить цены» перечитывает явно, ручная правка
помечается `manual`. Та же цепочка в батч-виде — `prices_for_pairs`: ею отчёт
считает цены самих изделий, `analog_prices` — обёртка над ней на одну пару.

**Отчёт `/cost/reg713`** (`pages/cost/reg713.vue`, кнопка «Согласование 713»
на главной): все изделия с `required=true` (галочка «и снятые» — `all=1`),
колонки — дата отметки (`required_at`, ставится при включении галочки и не
чистится при снятии), модель, артикул, наименование, розница и опт изделия по
правилам таблицы, вид, аналог (модель, артикул, наименование, розница, опт из
снимка карточки), решение, дата решения, № письма, комментарий, кто менял.
Поиск, фильтры (вид, решение, наличие аналога, период отметки), сортировка по
любой колонке и выгрузка в Excel — на клиенте; Excel тем же способом, что в
остальном разделе (HTML под `application/vnd.ms-excel`, выгружается то, что
отфильтровано на экране). Читать могут все с `cost:view`, править — только из
карточки.

#### Закупная готовая продукция (пожелания № 6/7) — `app/purchase.py`

Ассортимент, закупаемый у сторонних производителей, в CostHistory не попадает
(проверено 03.09.2026: 0 моделей из 164). Его КПСС считается на портале БМ —
база `mfportal` на OLAP-сервере (`MFPORTAL_DATABASE`, те же `OLAP_*`
реквизиты, только чтение), таблицы `bm_chl_*`:

| Таблица | Что берём |
|---|---|
| `bm_chl_checklists` | планы с `production_type = 'заказ готовой'`: `plan_number`, поставщик, сезон, БМ |
| `bm_chl_checklist_items` | строки плана: модель, наименование, путь группы (→ Level 01–05), количество, дата выпуска, `calculation_json` |
| `bm_chl_jobs` | задания Fox по (план, модель): **артикул** (в строках плана его нет), `job_number`, количество, цвет |
| `bm_chl_checklist_extra` | страна плана; если пусто — угадывается по названию плана («…Китай…», «…Бангладеш…») |
| `bm_chl_ready_goods_value` / `_rate` | ставки по группе за 2026-07 / курс — **не используются**: `_rate` пустая, ставки уже лежат снимком в JSON строки |

`calculation_json`: `placement_price_usd`, `retail_vat_rub`,
`reference_snapshot{exchange_rate_rub, duty_pct, transport_pct,
certification_usd, testing_usd}`. Заполнен только у планов SS2027; строки без
него импорт пропускает и считает («нет расчёта в портале»), как и строки без
заданий Fox («нет артикула»).

**Формула КПСС — допущение, портал её не показывает:** цена × транспорт % →
логистика, цена × пошлина % → таможня, сертификация $ + тесты $ →
сертификация, итог × курс из снимка → BYN. Опт — по справочнику уровней от
розницы (`PRICE_TYPE3 → PRICE_TYPE1`).

**Куда пишется.** Калькуляции — в `cost_manual_calc` (одна строка на
модель+артикул+КПСС+план+задание, `source_calc_sign = 'ПОРТАЛ'`, признак строки
«закупка готовой»), поэтому они видны через `cost_data_all` без правки вьюхи.
**В строку идёт только итог — в «Основные материалы»** (решение заказчика),
остальные статьи нули. Раскладка (цена, курс, логистика, таможня,
сертификация в $ и BYN, розница, опт, полный снимок) — в `cost_purchase_cost`
по тому же ключу; она же понадобится ПФКСС по приходу, чтобы сравнивать план и
факт по статьям. Импорт идемпотентен (UPDATE существующих строк ПОРТАЛ по
ключу), каждый запуск — строка `cost_purchase_import_run` с причинами
пропусков. Кнопка «Импорт закупной» (ПЭО/админ) сначала делает `dry_run` и
показывает цифры в confirm.

**В таблице**: `purchase_source` накладывается на строки `/aggregated` (как
мультипаки); значок 🛒 вместо ⧉ копии, фильтр «🛒 Только закупная», окно
раскладки по клику (для ПФКСС — рядом план КПСС и отклонение по статьям).

**ПФКСС по приходу (этап 2)** — `components/CostPurchaseInvoice.vue`, кнопка
«Приход (ПФКСС)», таблицы `cost_purchase_invoice` / `_line` (миграция 0055).
Эталон — Excel заказчика `Invoice_MF8693.xlsx`, расчёт `purchase.distribute`
воспроизводит его до копейки (проверено 03.09.2026: 7,7078 $ / 22,9355 BYN,
доли пошлины 30,228 % и 28,253 %):

**Валюты и курсы — всё через бел. рубль** (решение заказчика 04.09.2026;
первая версия 03.09 считала через доллар с тремя курсами в шапке и отброшена,
потому что не давала задать валюту отдельной статье):

- у инвойса выбирается **валюта прихода** (валюта цены строк) и её курс, у
  **каждой статьи накладных — своя валюта и свой курс**; курсы берутся к
  бел. рублю из НБ РБ на дату прихода, распределение считается в рублях, а итог
  пересчитывается обратно в доллары по курсу USD той же даты;
- валюты: BYN, USD, EUR, RUB, KZT, UZS, CNY (`purchase.CURRENCIES`, ITEM_ID
  справочника `[DWH].[dim].[valuta]`; кратность `KRATN` у всех 1);
- курс — **`KURS_BANK`**, не `KURS`: второй это внутренний курс компании
  (USD 3,10 против 2,9756 у НБ РБ на 24.08.2026). Проверено по эталонному
  Excel: USD 2,9756 и CNY 0,444 совпали с его шапкой;
- **дату в MSSQL передавать объектом `date`, не строкой**: сервер с русской
  локалью понял `'2026-09-04'` как 9 апреля и молча вернул не те курсы;
- если на дату записи нет, берётся ближайшая предыдущая, и в ответе `as_of`
  показывает, какая (UI пишет «взяты ближайшие предыдущие»);
- накладные лежат в `cost_purchase_invoice_overhead` (kind, hs_code, сумма,
  валюта, курс, сумма BYN), а не колонками шапки: одну статью можно задать
  дважды в разных валютах (в эталоне транспорт частью в рос. рублях, частью в
  долларах). **Пошлина — сумма на каждый код ТН ВЭД**, строки появляются сами
  по кодам из строк прихода;
- строки: модель, артикул, план, задание, наименование, цвет, код ТН ВЭД,
  количество прихода, цена в валюте. Добавляются поиском среди загруженных
  закупных КПСС по трём полям «И» (№ плана, модель, артикул) с галочками и
  массовым добавлением («выбранные» / «все найденные»; уже добавленные в
  результатах не показываются), «все из выборки» или пустой строкой.
  **Код ТН ВЭД подтягивается сам** из `S_MODELI.KTNVED` по модели и артикулу
  (`POST /purchase/hs-codes`, заполнен у 95 % справочника; пустой — ввести
  руками, кнопка «⟳ коды ТН ВЭД» перезаписывает). Количество вводится с
  инвойса (в `/aggregated` объёма выпуска нет);
- распределение: на единицу = цена_руб × (статья_руб / Σ стоимости строк
  группы); для пошлины группа — код ТН ВЭД, для остальных — весь инвойс.
  Свёртка в хранимые статьи: Логистика = транспорт + СВХ, Таможня = пошлина +
  сбор, Сертификация. `preview` показывает раскладку и сверку «введено vs
  распределено», предупреждает о кодах без пошлины и об отсутствии курса
  доллара; ошибки (нет курса валюты прихода или статьи, пошлина по коду без
  строк, строка без количества) останавливают расчёт;
- «Применить» пересчитывает на сервере и создаёт по строке калькуляцию ПФКСС в
  `cost_manual_calc` (`source_calc_sign = 'ПРИХОД'`): основа — строка КПСС той
  же калькуляции, если есть; дата расчёта = дата прихода; в «Основные
  материалы» — итог в бел. рублях; раскладка — в `cost_purchase_cost`
  (`source = 'invoice'`, курсы в `cur_to_byn` / `usd_to_byn` и дата в
  `rate_date`).
  Окно 🛒 у такой строки показывает план КПСС рядом и отклонение по статьям.
  Применённый инвойс не правится и не удаляется — только новый приход.

## Базы данных (`app/db.py`)

### postgres-cost (asyncpg)
- Пул: `COST_DATABASE_URL`, min_size=1, max_size=8
- Инициализируется в `lifespan`, закрывается при shutdown
- Таблица: `cost_price_changes_audit` (аудит изменений)
- `cost_manual_calc` — калькуляции, созданные в приложении копией с другим
  признаком (миграция 0043). Структура совпадает с `cost_data_cache`.
- `cost_data_all` — представление: кэш источника плюс `cost_manual_calc`.
  ЧИТАЮЩИЕ запросы раздела идут в него; загрузка и обновление кэша, применение
  наборов цен по плану — напрямую в `cost_data_cache`.
- `cost_multipack` / `cost_multipack_item` / `cost_multipack_build` — состав
  мультипаков и журнал сборок (миграция 0049, код `app/multipack.py`). Строк
  расчёта здесь нет: они живут в версии калькуляции.

### MSSQL Checks (pyodbc, read-only)
- `get_mssql_conn()` — чтение `[Checks].[CostHistory]` (~13M строк)
- `get_gpartner_conn()` — чтение `[Gpartner].[s_price_level]`

### MSSQL OLAP (pyodbc, write)
- `get_olap_conn()` — запись в `[FinSandBox].[CostHistory_Changes]`

### Драйвер
- `MSSQL_DRIVER` (по умолчанию `{ODBC Driver 18 for SQL Server}`)
- Все pyodbc-коннекты синхронные, FastAPI запускает их в threadpool
- Параметры: `TrustServerCertificate=yes; Encrypt=optional;`

### Источники данных (extern)

| Source | Host | DB | Доступ |
|--------|------|----|--------|
| MSSQL | `10.10.6.107` | `Checks` (CostHistory) | RO |
| MSSQL | `10.10.6.107` | `Gpartner` (price levels) | RO |
| MSSQL (OLAP) | `10.10.6.15` | `FinSandBox` (changes receiver) | RW |

## Mock-режим

`COST_MOCK=1` в `.env` — все эндпоинты отдают данные из `app/mocks.py` вместо MSSQL/OLAP.
- Фильтры: статический словарь `FILTER_OPTIONS` с 14 моделями и 15 артикулами
- Агрегаты: 136 сгенерированных строк
- Детали: 2 варианта на модель/артикул
- Уровни цен: 5 предопределённых уровней
- Сохранение: пишет только в локальный postgres-cost (аудит)

Нужен, когда у разработчика нет VPN до `10.10.6.107/15`.

## Nuxt-layer (`nuxt-layer/`)

Слой подключается через `extends: ['../python/cost/nuxt-layer']` в основном `nuxt.config.ts`.

### Страница `/cost` (`pages/cost/index.vue`)
Single-page приложение (template + script + scoped styles):
- **Шапка**: заголовок «Себестоимость», индикатор MOCK-режима
- **Фильтры**: 12 каскадных мультиселектов (CostMultiSelect), период дат, кнопки «Сбросить» / «Загрузить данные»
- **Action bar**: инфо о количестве записей, кнопки «Экспорт в Excel» и «Сохранить изменения»
- **Error banner**: показывается при ошибках API, с подсказкой про COST_MOCK=1
- **Таблица (агрегаты)**: 24 колонки + 🔍, пагинация по 50 строк, выбор уровня цен (dropdown)
- **Модальное окно детализации**: открывается по 🔍, с фильтрами дат/признака калькуляции, каскадными фильтрами колонок, таблицей 17 колонок, кнопкой «Открыть в новом окне»
- **Excel export**: генерирует HTML-таблицу → Blob → скачивание `.xls`
- **Ctrl+C**: копирует всю таблицу как TSV (если нет выделения)

### Хидеры запросов
- `apiBase` — пустая строка если `costOnly`, иначе `config.public.apiBase`

### Файлы слоя
- `nuxt.config.ts` — пустой (`export default defineNuxtConfig({})`)
- `CostMultiSelect.vue` — мультиселект компонент для фильтров
- `cost-redirect.global.ts` — middleware редиректа
- `cost-bypass.ts` — плагин для cost-only режима

## Миграции (`migrations/`)

- `0001_cost_init.up.sql` — создаёт таблицу `cost_price_changes_audit` (id BIGSERIAL, model, articul, price_level, retail_rub, wholesale_rub, username, changed_at)
- `init-db.sh` — запускается postgres-cost при первом старте: применяет все `*.up.sql` из `migrations/` через `psql`

## Dockerfile (`Dockerfile.dev`)

- Base: `python:3.12-slim-bookworm` (фиксирован, т.к. `-slim` уехал на trixie где строже keyring path)
- Устанавливает: `msodbcsql18` (ODBC для SQL Server), unixodbc, gcc/g++ (для сборки pyodbc)
- Копирует `requirements.txt`, устанавливает зависимости
- `CMD`: `uvicorn app.main:app --host 0.0.0.0 --port 8091 --reload --reload-dir /var/www/cost/app`
- `--reload-dir` сужен до `app/` — uvicorn не дёргается на изменения в `nuxt-layer/`

## Зависимости (`requirements.txt`)

```
fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
pydantic==2.9.2
pyodbc==5.1.0
python-dotenv==1.0.1
```

## Известные пробелы

- **Нет тестов.** Ни одного теста в `python/cost/`.
- **Нет линтинга/форматирования.** Ни ruff, ни black, ни mypy.
- **Нет схем Pydantic.** Роуты принимают `dict` вместо моделей — нет валидации на уровне типов.
- **sync DB вызовы в async FastAPI.** pyodbc синхронный, FastAPI запускает в threadpool — но при ошибках соединения нет таймаутов и retry-логики.
- **Ошибки соединения с MSSQL/OLAP.** Если сервер недоступен, endpoint падает с 500 без внятного сообщения.
- **Нет пула для pyodbc.** Каждый запрос открывает новый коннект к MSSQL/OLAP.
- **Жёстко зашитые SQL запросы.** Все в `routes.py`, нет query builder или ORM.
- **Excel export.** Генерирует HTML, не настоящий `.xlsx`.

