# SPEC — ВГО-отчёт: переход источника на `Table_Fin_PL`

> Статус: finpl реализован (T1–T8) как **opt-in** `DEBT_BACKEND=finpl`; дефолт
> возвращён на `mssql` (Premaster) до сверки сумм на наполненной витрине. Ветка
> `add-action`. Дата: 2026-06-24.
> Скоуп: отчёт «Задолженность ВГО» (`/api/reports/debt/*`), Go-слой `go/internal/reports/debt/`.

---

## 1. Objective (цель)

Сменить **первоисточник** ВГО-отчёта на каноническую витрину **`FinDWH.dbo.Table_Fin_PL`**,
которая наполняется «по всем свежим алгоритмам» (хранимка `dbo.Update_Table_Fin_PL`,
обновлена 22.06.2026). Из неё берём **всё, что в ней есть**; то, чего в ней нет,
**оставляем на `Premaster1C`** как цепочку-fallback (формулировка заказчика:
«берём с премастера всё — только если чего-то не хватает, оставь для цепочки данных»).

Практический эффект: **выручка / обороты ОПУ и признак ВГО канонизируются** (Table_Fin_PL),
а **сальдо ДЗ/КЗ, договор, срок оплаты/просрочка** остаются на Premaster (как сейчас).

### Зачем
- Сегодня выручка считается в Go из проводок Premaster + оверлей `[001 Mapping PL by BK]`/`[002 CodePL]`
  (`chart_of_accounts.go`, `LoadRevenueOverlay`). Это дублирует логику финотдела и расходится с
  «официальной» ОПУ. `Table_Fin_PL` уже содержит финальную классификацию `CodePL/GroupPL/GroupPL_new`,
  ручные корректировки (Блоки 5–7), переброски налогов — то, что Go повторить не может.
- Признак ВГО в Table_Fin_PL (`[ВГО]`) считается апстримом и шире нашего хардкода (см. §4.3).

---

## 2. Контекст данных (выяснено прямым зондированием OLAP `10.10.6.15` / `FinDWH`)

### 2.1 `Table_Fin_PL` — что это
- **USER_TABLE**, 34 колонки, **месячная** гранулярность (`Month DATE`), данные **с 2025-01-01**.
- В dev-снэпшоте FinDWH таблица **пустая (0 строк)** — наполняется по расписанию в prod
  через `TRUNCATE + INSERT` в `Update_Table_Fin_PL`. → Локально тестировать только через mock
  (`DEBT_MOCK=1`) или против prod-данных.
- Это витрина **ОПУ (P&L)**, не балансовые остатки. Есть BS-строки, но как **обороты**, не сальдо.

**Цепочка наполнения:**
```
Premaster1C ──► GLMF (proc) ──► VIEW vGLMFAddUSD ──┐
Budgeting.dbo.ZATRATY_DLYA_OKSANY_BYSTRYE ─────────┤  Update_Table_Fin_PL
[002 List of adj-plan] / [002 List of adj-ts] / ───┤  (TRUNCATE+INSERT,
 Adj001SamovyvozInternetShop (ручные правки) ──────┘   Блоки 1–7) ──► Table_Fin_PL
```

### 2.2 Колонки `Table_Fin_PL` (ключевые)
| Колонка | Смысл | Берём для |
|---|---|---|
| `Country` | страна (BY/RU/KZ/UZ…) | DebtRow.Country |
| `Month` (date) | месяц | период (фильтр/группировка) |
| `Компания` | **короткий код** ЮЛ (MF, F, TDMF, MFKaz, MFT…) | DebtRow.Company (через map код→ИНН) |
| `Корр-ка` (`DbAnalytics1`) | корр-аналитика (часто NULL) | вспом. |
| `CounterpartyName` | имя контрагента | DebtRow.Partner |
| `[ВГО]` (int 0/1) | **канонический флаг ВГО** | фильтр ВГО |
| `CodePL`, `GroupPL`, `GroupPL_new` | классификация ОПУ | определение выручки |
| `AmountBYN`, `Amount`, `AmountUSD`, `AmountRUB` | суммы (мультивалюта, без НДС, факт) | выручка/обороты |
| `DrAcc`, `CrAcc`, `Dr_Cr` | счета/направление | вспом. классификация |
| `CFO/CodeCFO/GroupCFO1..3`, `Scenario`, `Блок` | разрезы ЦФО / сценарий | потенциальные доп. разрезы |

### 2.3 Чего в `Table_Fin_PL` НЕТ → остаётся на Premaster (цепочка)
- **Сальдо ДЗ/КЗ** (входящее/оборот/исходящее) — балансовые остатки 62/60/76/1210…
- **Договор** (ref + имя) — субконто Premaster + `Objects`.
- **Срок оплаты / плановая дата / просрочка** — `Payments.dbo.Docs`.
- **ИНН контрагента** (есть только имя/код) и **менеджер/канал** — `Counterparty1C`.
- **Дневная** детализация (только месяц).

### 2.4 `vGLMFAddUSD` (на уровень ниже, если Table_Fin_PL не хватит)
Содержит `DocID/BaseDocID`, `CompanyID`, `CounterpartyID` (= **ИНН**, см. §4.1), `Date` (дневная),
бюджет/стратегию. **Но** договора, срока и сальдо ДЗ/КЗ тоже нет.

---

## 3. Решения (согласовано с заказчиком)

| # | Вопрос | Решение |
|---|---|---|
| 1 | Расположение Table_Fin_PL | `FinDWH.dbo` — те же креды `MSSQL_PREMASTER_*` |
| 2 | Сальдо ДЗ/КЗ (в Table_Fin_PL нет) | **из Premaster** (fallback-цепочка). Отчёт остаётся ДЗ/КЗ-ориентированным |
| 3 | Роль Premaster1C | **fallback только для недостающих полей** |
| 4 | Гранулярность | **месячная**; нижняя граница периода — 2025-01 |
| 5 | Матчинг юрлиц | по имени/коду (`Компания`→ИНН), ВГО — по флагу `[ВГО]=1`. См. §4 |
| 6 | Дримдом и ВГО-компании сверх 15 | **строим полный справочник** (§4.2), включаем все ВГО-ЮЛ |
| 7 | Дневная детализация | **согласно PL** → всё месячное; drilldown тоже из `Table_Fin_PL` (§5.1) |
| 8 | `DEBT_BACKEND=finpl` | **источник по умолчанию** (не opt-in) |

---

## 4. Архитектурные находки, влияющие на реализацию

### 4.1 `CompanyID`/`CounterpartyID` в `vGLMFAddUSD` = ИНН/УНП
Проверено: `690591512=MF`, `6950135110=TDMF`, `141240004842=MFKaz`, `9909349268=MFT`,
`5031159833=MFTex`, `9731039708=PTIR`, `305554644=MFUz`, `310170662=BR` — совпадают с `OurINNs()`.

### 4.2 В финальной `Table_Fin_PL` ID потеряны — есть только код `Компания`
Нужен **справочник `код → {ИНН, Name, Country}`** (расширить `Entity` полем `Code` в `seed.go`).
Полный список наших ЮЛ из `vGLMFAddUSD` (вся история, авторитетно):

| Код | ИНН | Страна | Имя | В seed-15? |
|---|---|---|---|---|
| `DR`    | 692221084 | BY | Дримдом ООО | **+ добавить** |
| `DR2`   | 693335015 | BY | Дримдом 2 ООО | **+ добавить** |
| `GP`    | 190465888 | BY | *(уточнить, 20 строк)* | **+ добавить** |
| `F`     | 690719790 | BY | ООО «Формэль» | да |
| `MF`    | 690591512 | BY | ООО «Марк Формэль» | да |
| `MFKaz` | 141240004842 | KZ | ТОО «Mark Formelle Kazakhstan» | да |
| `MFT`   | 9909349268 | RU | Филиал ООО «Марк Формэль» | да |
| `MFTex` | 5031159833 | RU | ООО «МАРК ФОРМЭЛЬ ТЕКС» | да |
| `PTIR`  | 9731039708 | RU | ООО «ПТИР» | да |
| `TDMF`  | 6950135110 | RU | ООО ТД «Марк Формэль» | да |
| `BR`    | 310170662 | UZ | «BARREIROS SOFT» MCHJ | да |
| `MFUz`  | 305554644 | UZ | ООО «MARK FORMELLE IT» | да |

Также ICO-контрагент с `ИНН=NULL`: **Летникова Е.Г.** («аренда квартиры») — реальная строка
с `[ВГО]=1` без сматчиваемого ИНН (§11.6). Зарубежные ЮЛ из seed (TR/CZ/GB/CN/KG +
ИП Сипарова 695018688905) в PL-данных почти не фигурируют — в справочнике остаются, кода нет.

### 4.3 Канонический `[ВГО]` шире хардкода `OurINNs` — РЕШЕНО: строим полный справочник
В данных `[ВГО]=1` встречаются **DR/DR2 (Дримдом, Дримдом 2)** и **GP** — их **нет** в seed-15.
Решение (вопрос #6): **расширяем справочник** всеми ВГО-ЮЛ из таблицы §4.2.
Фильтр ВГО упрощается до `WHERE [ВГО]=1` (заменяет `vgoMSSQLClause`) — это **поведенческое
изменение**: в отчёт официально попадают внутригрупповые компании сверх текущих 15.

---

## 5. Маппинг `DebtRow` → источник (provenance)

| Поле DebtRow | Источник | Примечание |
|---|---|---|
| Country | **Table_Fin_PL.Country** | primary |
| Company / CompanyINN | **Table_Fin_PL.Компания** → map код→ИНН/Name | §4.2 |
| Partner | **Table_Fin_PL.CounterpartyName** | primary |
| PartnerINN | Premaster (`Counterparty1C`) | цепочка; в Table_Fin_PL нет |
| Manager / Channel | Premaster (`Counterparty1C`) | цепочка |
| RevenuePeriod / RevenueLastMonth | **Table_Fin_PL** (CodePL/GroupPL='ПРОДАЖИ', Amount* по Month) | **канонизируется** |
| Currency + суммы | **Table_Fin_PL** (AmountBYN/Amount/USD/RUB) | мультивалюта встроена |
| OpeningDZ/KZ, TurnoverDZ/KZ, ClosingDZ/KZ | Premaster (сальдо 62/60/76) | цепочка (§2.3) |
| Contract / ContractRef | Premaster (`Objects`) | цепочка |
| PaymentTermDays / PaymentDueDate / OverdueDays | Premaster (`Payments.Docs`) | цепочка |

> Итого: **выручка/ОПУ + признак ВГО + страна/контрагент-имя** ← Table_Fin_PL;
> **ДЗ/КЗ-сальдо + договор + просрочка + ИНН/менеджер/канал** ← Premaster.

### 5.1 Drilldown — «согласно PL» (решение #7)
Гранулярность **всего отчёта и детализации — месячная**. Drilldown больше **не ходит в Premaster
по дням**; детализация берётся из **`Table_Fin_PL`** — строки, свёрнутые в месячный агрегат:
`Month, DocName1C, OperationDescription, CodePL/GroupPL, Dr_Cr (DrAcc/CrAcc), AmountBYN/Amount/USD/RUB`.
→ `DocumentRow` переосмысливается как **PL-строка месяца** (а не проводка по дате). Дневной
`premasterRepo.Drilldown` в новом источнике **не используется** (остаётся для `DEBT_BACKEND=mssql`).

---

## 6. План реализации (предлагаемый)

Источник прячется за интерфейсом `PremasterRepo` (`service.go`) — бизнес-логика
`BuildReport`/`BuildDrilldown` не меняется (работает с `rawRow`).

1. **`seed.go`** — добавить `Code` в `Entity`; заполнить коды; **добавить DR/DR2/GP** (§4.2);
   helper `INNByCode(code)` (и обратный, если нужно). Зарубежные ЮЛ остаются без кода.
2. **Новый репозиторий** `repo_finpl.go` — `finPLRepo implements PremasterRepo`:
   - `Report()` строит выручочную/оборотную часть `[]DebtRow` из `Table_Fin_PL`
     (`WHERE [ВГО]=1 AND Month BETWEEN @from AND @to`, GROUP BY компания/контрагент/валюта),
     компания резолвится `Компания`→ИНН/Name через справочник.
   - сальдо ДЗ/КЗ, договор, просрочку, ИНН/менеджер/канал контрагента **дотягивает из Premaster**
     (композиция с существующим `premasterRepo`, по образцу `compositeRepo` из `repo_clickhouse.go`).
   - `Drilldown()` — **месячная PL-детализация из `Table_Fin_PL`** (§5.1), не Premaster-дни.
3. **Wiring** `main.go` + `config.go` — `DEBT_BACKEND=finpl` **по умолчанию** (решение #8);
   сохранить `mssql`/`ch`/`mock` как явные альтернативы. Новые env:
   - `MSSQL_FINPL_TABLE` (default `Table_Fin_PL`),
   - период-флор `DEBT_FINPL_MIN_MONTH` (default `2025-01-01`).
4. **`.env.example`** — задокументировать **обе** новые переменные + смену дефолта `DEBT_BACKEND`
   в том же коммите (см. §9).
5. **`mocks.go`** — фикстуры, отражающие месячную ОПУ-структуру (таблица пуста в снэпшоте).
6. **Период** — нормализовать `date_from`/`date_to` к границам месяца; клампить нижнюю границу 2025-01.
7. **Документация** — обновить `docs/reports/debt/*` и добавить запись в план.

---

## 7. Commands (для разработки и проверки)

```bash
# Контур (из swarm/)
make up                 # Go + Nuxt + PG + Redis
make restart-go-api     # перечитать env (см. memory: restart не перечитывает env сам)
make logs-go

# Локальный прогон ВГО без OLAP
DEBT_MOCK=1             # фикстуры из mocks.go (Table_Fin_PL пуст в снэпшоте)

# Зондирование OLAP (как делали при анализе) — одноразовый Go-probe в go/cmd/<tmp>,
# DSN: sqlserver://<user>:<pass>@10.10.6.15:1433?database=FinDWH&encrypt=true&trustservercertificate=true
# креды из корневого .env (MSSQL_PREMASTER_*). Probe удалять после использования.
```

Эндпоинты ВГО: `GET /api/reports/debt/{filter-options,report,drilldown,saved-filters}`
(роль `ROLE_FINANCE_ADMIN`).

---

## 8. Project structure (что трогаем)

```
go/internal/reports/debt/
├── seed.go            (+) Entity.Code, INNByCode, вопрос Дримдома
├── repo_finpl.go      (НОВЫЙ) finPLRepo + композиция с premasterRepo
├── service.go         (—) без изменений (интерфейс PremasterRepo)
├── model.go           (—) DebtRow без изменений; маппинг — в repo
├── mocks.go           (+) месячные ОПУ-фикстуры
├── vgo_report_filter.go (~) finpl-вариант: WHERE [ВГО]=1
go/internal/config/config.go   (+) MSSQL_FINPL_TABLE, DEBT_FINPL_MIN_MONTH, DEBT_BACKEND=finpl
go/cmd/api/main.go              (+) wiring finpl-репозитория
.env.example                    (+) обе новые переменные с комментариями
docs/reports/debt/*             (+) описание нового источника и провенанса
```

Frontend (`nuxt/`) — **не трогаем** (DebtRow/ReportResponse стабильны). Если уберём дневную
точность — обновить подписи периода в UI (минимально).

---

## 9. Code style / boundaries

### Always (делаем всегда)
- **Любая новая env → в `.env.example` в том же коммите** с комментарием (дефолт + последствие пустого).
  Иначе прод падает после деплоя (история с `MSSQL_PREMASTER_*`/`DEBT_MOCK`).
- Источник — только за интерфейсом `PremasterRepo`; `BuildReport` не дублировать.
- Идентификаторы таблиц/схем из env — через существующую валидацию (`PremasterTables.Validate`,
  анти-инъекция), параметры — именованные (`sql.Named`), как в `repo_premaster.go`.
- Диалог и комментарии — на русском (предпочтение пользователя).
- Дизайн-токены, если коснёмся UI, — только из `design-system.css` (без хардкода цветов).

### Решено заказчиком
- Дримдом/DR2/GP и прочие ВГО-ЮЛ — **в справочник** (§4.2–4.3).
- Детализация — **согласно PL** (месячная, drilldown из Table_Fin_PL; §5.1).
- `finpl` — **источник по умолчанию** (§6.3).

### Ask first (осталось уточнить по ходу)
- Имя/страна юрлица **`GP` (190465888)** — подтвердить (в данных только код).
- На какую дату месяца брать **closing-сальдо ДЗ/КЗ** из Premaster при стыковке с месячной ОПУ (§11.2).

### Never (не делаем)
- Не заворачивать `/api/*` finance-фасада в Go (контракт callback'а, см. CLAUDE.md).
- Не править cost-раздел (`python/cost/`) — он изолирован.
- Не коммитить временные probe-утилиты (`go/cmd/dbprobe` и т.п.) — удалять после анализа.
- Не «канонизировать» сальдо ДЗ/КЗ из Table_Fin_PL — там их нет (только обороты).

---

## 10. Testing strategy

- **Тестового раннера в репо нет** (см. CLAUDE.md). Единичные проверки — рядом, без общего раннера.
- **Mock-режим** (`DEBT_MOCK=1`) — основной способ локальной проверки: Table_Fin_PL пуст в снэпшоте.
- **Контрактная проверка**: ответ `ReportResponse`/`DebtRow` неизменен → старый UI работает без правок.
- **Сверка с финотделом**: суммы выручки ВГО из нового источника сверить с «официальным» PL-файлом
  (Table_Fin_PL — его витрина) на 1–2 месяцах 2025 г.
- **Граничные случаи**: период < 2025-01 (клампинг), компания вне map код→ИНН (Дримдом),
  месяц без данных, мультивалютные строки.

---

## 11. Открытые вопросы / риски (после решений #6–8)

1. **`GP` (190465888)** — имя/страна юрлица не подтверждены (в данных только код, 20 строк).
2. **Стыковка месячной ОПУ (Table_Fin_PL) и сальдо ДЗ/КЗ (Premaster)** — на какую дату месяца
   брать closing-сальдо; как соотносить ключи группировки (компания/контрагент/валюта vs договор).
3. **Прод-наполнение Table_Fin_PL** — расписание `Update_Table_Fin_PL`, свежесть, поведение при
   пустой таблице (graceful empty vs ошибка). В dev-снэпшоте таблица пуста → только mock.
4. **PartnerINN/менеджер/канал** при матчинге по имени `CounterpartyName` — насколько надёжно
   связывается с `Counterparty1C` (имена не нормализованы: «Марк Формэль ТД ООО, Р…»).
5. **Смена дефолта `DEBT_BACKEND` → `finpl`** — затрагивает prod; убедиться, что Table_Fin_PL
   наполнена до выката (иначе пустой отчёт). В `.env` сейчас `ch`.
6. **ICO-контрагент с `ИНН=NULL`** (Летникова, «аренда квартиры») — `[ВГО]=1`, но ИНН нет;
   решить отображение PartnerINN (NULL/пропуск).
```

